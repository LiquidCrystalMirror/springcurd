<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.springbootdemo.mapper.CustomerMapper">

    <insert id="insert" parameterType="org.example.springbootdemo.dto.CustomerDTO">
        INSERT INTO customer (id, name, password, status, role_id)
        VALUES (#{id}, #{name}, #{password}, 1, 1)
    </insert>

    <select id="getById" resultType="org.example.springbootdemo.vo.UserVO">
        SELECT id, name, status, role_id FROM customer WHERE id = #{id}
    </select>

    <select id="getPassword" parameterType="int" resultType="String">
        SELECT password FROM customer WHERE id = #{id}
    </select>

    <update id="update" parameterType="org.example.springbootdemo.dto.CustomerDTO">
        UPDATE customer SET name = #{name}, phone = #{phone}, address = #{address} WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE customer SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
