<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.springbootdemo.mapper.OrderDetailMapper">


    <!-- 根据主键查询 -->
    <select id="queryById" resultType="OrderDetail">
        SELECT id, order_no, platform_id, product_id, quantity, status, create_time, update_time
        FROM order_detail
        WHERE id = #{id}
    </select>

    <!-- 分页查询 -->
    <select id="find" resultType="OrderDetail" parameterType="OrderQuery">
        SELECT id, order_no, platform_id, product_id, quantity, status, create_time, update_time
        FROM order_detail
        <where>
            <if test="orderNo != null and orderNo != ''">
                AND order_no = #{orderNo}
            </if>
            <if test="platformId != null and platformId != ''">
                AND platform_id = #{platformId}
            </if>
            <if test="productId != null">
                AND product_id = #{productId}
            </if>
            <if test="quantity != null">
                AND quantity = #{quantity}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <!-- 插入（ID由Java代码生成） -->
    <insert id="insert" parameterType="OrderDetail">
        INSERT INTO order_detail (id, order_no, platform_id, product_id, quantity, status, create_time, update_time)
        VALUES (#{id}, #{orderNo}, #{platformId}, #{productId}, #{quantity}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="OrderDetail">
        UPDATE order_detail
        <set>
            <if test="orderNo != null and orderNo != ''">order_no = #{orderNo},</if>
            <if test="platformId != null and platformId != ''">platform_id = #{platformId},</if>
            <if test="productId != null">product_id = #{productId},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据主键删除 -->
    <delete id="deleteById">
        DELETE FROM order_detail WHERE id = #{id}
    </delete>

    <!-- 根据订单号和平台ID将明细状态更新为已回滚 -->
    <update id="updateStatusToRollback">
        UPDATE order_detail
        SET status = 2,
            update_time = NOW()
        WHERE order_no = #{orderNo}
          AND platform_id = #{platformId}
    </update>



    <!-- 根据订单号、平台ID和商品ID查询订单明细 -->
    <select id="findByOrderAndProduct" resultType="OrderDetail">
        SELECT id, order_no, platform_id, product_id, quantity, status, create_time, update_time
        FROM order_detail
        WHERE order_no = #{orderNo}
          AND platform_id = #{platformId}
          AND product_id = #{productId}
    </select>

    <!-- 根据订单号、平台ID和商品ID更新状态为已取消（部分退货） -->
    <update id="updateStatusToCancelByProduct">
        UPDATE order_detail
        SET status = 0,
            update_time = NOW()
        WHERE order_no = #{orderNo}
          AND platform_id = #{platformId}
          AND product_id = #{productId}
          AND status = 1
    </update>

</mapper>