<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.springbootdemo.mapper.StaffMapper">

    <select id="find" resultType="org.example.springbootdemo.vo.UserVO" parameterType="org.example.springbootdemo.query.UserQuery">
        SELECT id, name, status, role_id FROM staff
        <where>
            <if test="name != null and name !=''"> AND name LIKE CONCAT('%', #{name}, '%') </if>
            <if test="status != null and status.length > 0">
                AND status IN
                <foreach item="item" collection="status" open="(" separator="," close=")"> #{item} </foreach>
            </if>
            <if test="roleId != null and roleId.length > 0">
                AND role_id IN
                <foreach item="item" collection="roleId" open="(" separator="," close=")"> #{item} </foreach>
            </if>
        </where>
    </select>

    <select id="getById" resultType="org.example.springbootdemo.vo.UserVO">
        SELECT id, name, status, role_id FROM staff WHERE id = #{id}
    </select>

    <select id="getDetailById" resultType="org.example.springbootdemo.vo.UserDetailVO">
        SELECT id, name, status, role_id, creator_id, approver_id, created_at, updated_at, approve_time
        FROM staff
        WHERE id = #{id}
    </select>

    <insert id="insertWithAudit">
        INSERT INTO staff (id, name, password, status, role_id, creator_id, created_at, updated_at)
        VALUES (#{id}, #{name}, #{password}, 2, #{roleId}, #{creatorId}, NOW(), NOW())
    </insert>

    <update id="approve">
        UPDATE staff
        SET status = #{status},
            approver_id = #{approverId},
            approve_time = NOW(),
            updated_at = NOW()
        WHERE id = #{id}
          AND status = 2
    </update>

    <update id="update" parameterType="org.example.springbootdemo.dto.StaffDTO">
        UPDATE staff
        SET name = #{name}, status = #{status}, role_id = #{roleId}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM staff WHERE id = #{id}
    </delete>

    <select id="getPassword" parameterType="int" resultType="String">
        SELECT password FROM staff WHERE id = #{id}
    </select>

</mapper>
