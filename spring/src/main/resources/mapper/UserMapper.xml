<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.springbootdemo.mapper.UserMapper">
    <!--定义查询全部
    id 为方法名
    resultType 一条记录封装成什么类型的对象
    -->
    <select id="getUserList" resultType="UserVO">
        SELECT id, name, status, role_id FROM user
    </select>

    <select id="getUserById" resultType="UserVO">
        SELECT id, name, status, role_id FROM user WHERE id = #{id}
    </select>

    <select id="getUserDetailById" resultType="org.example.springbootdemo.vo.UserDetailVO">
        SELECT id, name, status, role_id, creator_id, approver_id, created_at, updated_at, approve_time
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="insertUser" parameterType="UserDTO">
        INSERT INTO user (id, name, password, status, role_id)
        VALUES (#{id}, #{name}, #{password}, 1, #{roleId})
    </insert>

    <!-- 人事创建用户：status=2(待审核), 写入creator_id和创建/更新时间 -->
    <insert id="insertUserWithAudit">
        INSERT INTO user (id, name, password, status, role_id, creator_id, created_at, updated_at)
        VALUES (#{id}, #{name}, #{password}, 2, #{roleId}, #{creatorId}, NOW(), NOW())
    </insert>

    <!-- 监管审核用户：更新status/approver_id/approve_time（必须原状态=2待审核） -->
    <update id="approveUser">
        UPDATE user
        SET status = #{status},
            approver_id = #{approverId},
            approve_time = NOW(),
            updated_at = NOW()
        WHERE id = #{id}
          AND status = 2
    </update>

    <delete id="deleteUser" parameterType="int">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <update id="updateUser" parameterType="UserDTO">
        UPDATE user SET name = #{name}, status = #{status}, role_id = #{roleId} WHERE id = #{id}
    </update>

    <select id="getPassword" parameterType="int" resultType="String">
        SELECT password FROM user WHERE id = #{id}
    </select>

    <select id="find" resultType="UserVO" parameterType="UserQuery">
        select id,name,status,role_id from user
        <where>
            <if test="name != null and name !=''"> and name like concat('%', #{name}, '%') </if>
            <if test="status != null and status.length > 0">
                and status in
                <foreach item="item" collection="status" open="(" separator="," close=")"> #{item} </foreach>
            </if>
            <if test="roleId != null and roleId.length > 0">
                and role_id in
                <foreach item="item" collection="roleId" open="(" separator="," close=")"> #{item} </foreach>
            </if>
        </where>
    </select>

</mapper>